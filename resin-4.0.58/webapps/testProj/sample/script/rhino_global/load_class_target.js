var loadingMills = (new Date().getTime());
Debug.print("[" + loadingMills + "]　load_class_target.jsがロードされました。");

function functionOfLoadClassTarget(){
	Debug.print("[" + loadingMills + "]　load_class_target.js内で定義されている関数が実行されました。");
}
